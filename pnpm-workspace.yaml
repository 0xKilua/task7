packages:
  - "apps/*"
  - "packages/*"
  # apps/mobile est un projet Expo/React Native gere avec npm (voir sa
  # propre documentation) : Metro Bundler et les symlinks pnpm ne
  # cohabitent pas bien, c'est l'approche standard recommandee par Expo
  # pour les monorepos.
  - "!apps/mobile"
